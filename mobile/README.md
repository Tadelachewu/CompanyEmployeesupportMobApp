# Mobile (Expo)

Run:

```bash
cd mobile
npm install
npx expo start
```

Notes:
- If running on a physical device, use the Expo Go app and ensure the backend is reachable from your device (use your machine IP address instead of localhost if needed).
